.DS_Store
.hugo_build.lock
# LaTeX build files for the CV (the served copy lives in static/CV.pdf)
cv/*.aux
cv/*.log
cv/*.out
cv/*.pdf
cv/build.log

# Hugo output: rebuilt by GitHub Actions on every push, never committed
public/
resources/_gen/
